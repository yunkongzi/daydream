<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kh.daydream.mappers.review">
	
	<!-- 리뷰글 추가 -->
	<insert id="insertReview">
		insert into tbl_review(bno, user_id, title, content, class_no)
		values(#{bno}, #{user_id}, #{title}, #{content}, #{class_no})
	</insert>
	<!-- 리뷰  정보 보기 -->
	<select id="selectById" resultType="ReviewVo">
		selct * from tbl_review
		where bno = #{bno}
	</select>
	
	<!-- 리뷰 수정하기 -->
		<update id="updateReview">
			update tbl_review set
				user_id = #{user_id},
				title = #{title},
				content = #{content}
			where 
		</update>

</mapper>