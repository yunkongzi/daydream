<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper			
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"			
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kh.daydream.mappers.reservation">

	<!-- 클래스 목록 -->
	<!-- <select id="classList">
		select * from tbl_reservation
		where class_no = #{class_no}
		order by class_no desc
	</select> -->
	
	<!-- 예약등록 -->
	<insert id="insertReservation">
		insert into tbl_reservation(user_id, res_date, rno, class_sub_no, count, status)
		values(#{user_id}, #{res_date}, seq_rno.nextval, #{class_sub_no}, #{count}, #{status})
	</insert>
	
	<!-- 클래스 시간 목록 (1~4) -->
   <select id="selectTimeList" resultType="ClassTimeVo">
   		select * from tbl_class_time
		order by time_no
   </select>

</mapper>